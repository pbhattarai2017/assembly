## assembly
This contains example assembly language programs written for nasm (netwide assembler) for 64-bit machine.

#### Execution
1. `$ chmod +x maker.sh`
2. `$ ./maker.sh <program>` (program is a file in /src/; donot input .asm extension)
