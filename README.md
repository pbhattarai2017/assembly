## assembly
This contains example assembly language programs written for nasm (netwide assembler) for 64-bit machine.<br>
*src/* contains numerous single file assembly programs.

#### Execution
1. `$ chmod +x maker.sh`
2. `$ ./maker.sh <program>` (program is a file in src/; do not input .asm extension)
